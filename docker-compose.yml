version: '1'
services:
  flask-app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app  # バインドマウント: ホストのカレントディレクトリをコンテナ内の/appにマウント
    environment:
      FLASK_ENV: development  # 開発環境の場合はこれを設定